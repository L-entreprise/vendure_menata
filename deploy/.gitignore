# Dependencies & build outputs (regenerated by the Docker build)
node_modules
dist
graphql

# Runtime/dev artifacts
assets
test-emails
*.log
_build*.log

# Local env (inject via Coolify env vars; never commit real secrets)
.env
.env.*
!.env.example
